# Hostname/IP and port of the weather-hub instance this sensor reports to.
# Both are optional - if unset, the sensor falls back to the "home-hub" LAN
# hostname and port 3001.
SENSOR_SERVER_IP=home-hub
SENSOR_SERVER_PORT=3001
